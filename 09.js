var helpers = require("./helpers");

const part1 = (data) => {
  const stuff = helpers.splitByNewLine(data);
  stuff.forEach((item) => {});
  return 0;
};

const part2 = (data) => {
  const stuff = helpers.splitByNewLine(data);
  stuff.forEach((item) => {});
  return 0;
};

module.exports = {
  part1,
  part2,
};
