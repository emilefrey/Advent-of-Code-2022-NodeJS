var helpers = require("./helpers");

const part1 = (data) => {
  const stuff = helpers.splitByNewLine(data);
  let total = 0;

  stuff.forEach((a) => {});
  return total;
};

const part2 = (data) => {
  const stuff = helpers.splitByNewLine(data);
  let total = 0;

  stuff.forEach((a) => {});
  return total;
};

module.exports = {
  part1,
  part2,
};
